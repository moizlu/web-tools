// import type { ToolName } from "$lib/types"
// import { z } from "zod"

// export const loadSavedData = (name: ToolName , pageState, schema: z.ZodObject) => {
//     try {
//         const savedData = localStorage.getItem(name);
//         if (savedData)
//     }
// }
